#include "vector.hpp"

int main()
{
    ft::vector<int> vector_ft;

/*    std::cout << "ft::vector.empty(): " << vector_ft.empty() << std::endl << std::endl;

    for (int i = 0; i <= 10; i++)
        vector_ft.push_back(i);
    
    ft::stack<int, ft::vector<int> > stack2_ft(vector_ft);

    std::cout << "ft::stack.empty(): " << stack2_ft.empty() << std::endl << std::endl;
*/    return (0);
}